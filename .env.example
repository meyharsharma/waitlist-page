# ---- Database (Supabase Postgres) ----
DATABASE_URL=

# ---- Supabase project ----
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_ANON_KEY=

# ---- Rate limiting (Upstash Redis) ----
# From the Upstash console (or the Vercel + Upstash integration).
# When unset, the signup endpoint falls back to a best-effort in-memory limiter.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ---- Local server ----
PORT=56321
